#!/usr/bin/env bash
set -euo pipefail
cd "$(dirname "$0")/.."
mkdir -p config/custom_components
cp -R custom_components/sungrow_shx_inverter config/custom_components/
exec .venv/bin/hass --config "$PWD/config" --debug
